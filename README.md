# dom-attic

Монтируй тяжёлые компоненты один раз, показывай дешёвые снимки, оживляй по касанию.

## Зачем

Обычная виртуализация списков экономит DOM ценой перемонтирования: строка ушла за экран —
компоненты уничтожены, вернулась — созданы заново. Если ячейка тяжёлая (селект на 10 000
опций, редактор, график), скролл превращается в непрерывную пересборку.

`dom-attic` разворачивает подход: компонент монтируется **один раз за всю жизнь страницы**
и больше никогда не пересоздаётся. Когда он не нужен — его DOM-узел уезжает в detached-контейнер
(там нет ни layout, ни paint, но компонент жив и продолжает получать обновления),
а на его месте остаётся инертный снимок — `cloneNode` живого узла.

Пользователь касается снимка — тот мгновенно уступает место настоящему компоненту
со всем его состоянием.

## Установка

```sh
npm i dom-attic
```

## Ядро (без фреймворка)

```ts
import { Attic, attachInteraction } from 'dom-attic'

const attic = new Attic({ liveLimit: 20 })

attic.register('row-1:status', hostElement)  // host уже содержит смонтированный узел
attic.park('row-1:status')                   // узел в хранилище, на месте — снимок

const detach = attachInteraction(attic, tableRoot)  // один слушатель на весь список
```

| Метод | |
|---|---|
| `register(key, host)` | взять ячейку под управление |
| `park(key)` | увести живой узел, оставив свежий снимок |
| `revive(key)` | вернуть живой узел, вытеснив давние сверх `liveLimit` |
| `markDirty(key)` / `refresh(key)` | данные изменились → переснять снимок |
| `dispose()` | вернуть всё в документ (размонтирует фреймворк) |

## Vue

```vue
<script setup>
import { AtticCell, useAtticRoot } from 'dom-attic/vue'

const { rootRef, stats } = useAtticRoot({ liveLimit: 20 })
</script>

<template>
  <div ref="rootRef">
    <AtticCell
      v-for="row in rows"
      :key="row.id"
      :cell-key="row.id"
      :revision="row.updatedAt"
    >
      <HeavySelect v-model="row.status" :options="tenThousand" />
    </AtticCell>
  </div>
</template>
```

React-адаптер планируется той же схемой: ядро о фреймворке ничего не знает.

## Ограничения

- содержимое ячейки должно быть **однокорневым** элементом — снимается только первый узел;
- `canvas` и `video` не переносятся в снимок: таким ячейкам `parkOnMount: false`;
- JS-тултипы и прочие эффекты, живущие вне ячейки, на снимке не работают до оживления;
- ячейку с фокусом внутри не следует вытеснять — держите `liveLimit` с запасом.

## Разработка

```sh
npm run dev        # playground
npm test           # юнит-тесты ядра в настоящем браузере
npm run test:e2e   # сценарии взаимодействия
npm run build
```

## Лицензия

MIT
